# Fetch Followers Count

## Endpoint

**URL:** `/:projectId/users/:userId/followers-count`

**Method:** `GET`

**Authentication Required:** No

---

## Description

Retrieve the number of followers a specific user has within a given project.

---

## Request

### URL Parameters

| Parameter | Type     | Required | Description                                                |
| --------- | -------- | -------- | ---------------------------------------------------------- |
| `userId`  | `string` | Yes      | The ID of the user whose followers count is being fetched. |

### Headers

_None_

### Body Parameters

_None_

#### Example Request

```sh
GET /proj1234/users/67890/followers-count
```

---

## Response

### Success Response (200 OK)

```json
{
  "count": 42
}
```

### Error Responses

#### Missing or Invalid User ID (400 Bad Request)

```json
{
  "error": "Missing or invalid userId in request parameters",
  "code": "user/invalid-user-id"
}
```

#### Server Error (500 Internal Server Error)

```json
{
  "error": "Internal server error",
  "code": "user/server-error",
  "details": "<Error message>"
}
```

---

## Notes

- This endpoint does **not** require authentication.
- Only the follower count is returned, not individual follower details.
